name: Terraform Deployment by a container

on:
  workflow_dispatch:
    inputs:
      container_name:
        description: 'Select the container to deploy'
        required: false
        type: choice
        options: [none, infterraform, infterraformdr]
        default: 'none'
      target_env:
        description: "Select Environment (dev, uat, sit, production, drhub)"
        required: true
        type: choice
        options: [dev, uat, sit, production, drhub]
        default: "production"
      working_directory:
        description: "Terraform folder path"
        required: true
        type: choice
        options: [
           dr/rg
           
        ]
          

# Global Environment Variables
env:
  BACKEND_SUB_ID: ${{ secrets.BACKEND_SUBSCRIPTION_ID }}
  STG_NAME: ${{ secrets.BACKEND_STORAGE_ACCOUNT_NAME }}
  STG_CONT: ${{ (github.event.inputs.container_name != 'none' && github.event.inputs.container_name) || secrets.BACKEND_CONTAINER_NAME }}
  RG_NAME: ${{ secrets.BACKEND_RESOURCE_GROUP }}
  ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
  ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
  ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
  TF_INPUT: "0"
  TF_IN_AUTOMATION: "true"
  
permissions:
  id-token: write
  contents: read

jobs:
  setup:
    name: "Evaluate Environment Branch Rule"
    runs-on: [self-hosted, Windows]
    outputs:
      selected_branch: ${{ steps.determine_branch.outputs.branch }}
    steps:
      - name: Determine Branch
        id: determine_branch
        shell: powershell
        run: |
          $env_input = "${{ github.event.inputs.target_env }}"
          if ($env_input -eq "production" -or $env_input -eq "drhub") {
            $branch = "main"
          } else {
            $branch = $env_input
          }
          echo "branch=$branch" >> $env:GITHUB_OUTPUT
          Write-Host "Selected branch is: $branch"

  terraform_plan:
    name: "Plan - ${{ github.event.inputs.target_env }}"
    needs: setup
    environment: ${{ github.event.inputs.target_env }}
    runs-on: [self-hosted, Windows]
    outputs:
      plan_commit_sha: ${{ steps.capture_sha.outputs.sha }}
      plan_artifact_name: ${{ steps.name_artifact.outputs.art_name }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.setup.outputs.selected_branch }}

      - name: Capture Exact Commit SHA
        id: capture_sha
        shell: powershell
        run: |
          $sha = (git rev-parse HEAD).Trim()
          echo "sha=$sha" >> $env:GITHUB_OUTPUT

      - name: Name Artifact
        id: name_artifact
        shell: powershell
        run: |
          echo "art_name=tf-plan-${{ github.event.inputs.target_env }}-${{ github.run_id }}" >> $env:GITHUB_OUTPUT

      - name: Initialize Terraform Backend
        shell: powershell
        working-directory: ${{ github.event.inputs.working_directory }}
        run: |
          Write-Host "Connecting to Backend Storage in Sub: $env:BACKEND_SUB_ID"
          terraform init `
            -backend-config="subscription_id=$env:BACKEND_SUB_ID" `
            -backend-config="storage_account_name=$env:STG_NAME" `
            -backend-config="container_name=$env:STG_CONT" `
            -backend-config="resource_group_name=$env:RG_NAME" `
            -reconfigure

      - name: Terraform Validate & Plan
        shell: powershell
        working-directory: ${{ github.event.inputs.working_directory }}
        run: |
          $ErrorActionPreference = 'Stop'
          Write-Host "Generating Plan for Deployment Sub: $env:ARM_SUBSCRIPTION_ID"
          terraform validate
          terraform plan -out="tfplan.binary"
          terraform show -no-color "tfplan.binary" > plan_preview.txt
          Get-Content plan_preview.txt | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
